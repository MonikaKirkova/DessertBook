import { AnalyzedInstructions } from './analyzed-instructions';

describe('AnalyzedInstructions', () => {
  it('should create an instance', () => {
    expect(new AnalyzedInstructions()).toBeTruthy();
  });
});
